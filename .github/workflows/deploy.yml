name: Deploy

on:
  push:
    branches: [feature/mcp-server]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Copy env file
        run: cp /home/ubuntu/aycf-bot/.env .env

      - name: Deploy
        run: docker compose up --build -d
